package game;

import javax.swing.JPanel;

public class Prompt extends JPanel{
	//Prompt gets put on top of the view when a selection needs to be made
	//Prompt has to options to click in. Yes or No on whether the object belongs. 
	//It'll be a rough outline with a picture of the object inside
		
}
